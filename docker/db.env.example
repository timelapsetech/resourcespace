# Copy to docker/db.env and change every password before first start.
# Used by the MariaDB container and (for reference) documented in config.php.example.

MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_DATABASE=resourcespace
MYSQL_USER=resourcespace
MYSQL_PASSWORD=change-me-app
