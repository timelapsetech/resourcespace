1.0 Initial release — cadence ingest, proxy preview, representative frame, ZIP download
1.1 Team home link, group permission (is), offline job registration, setup uses admin perm, install polish
1.2 Representative frame extracts full still metadata (dimensions, DPI, size, camera/lens EXIF)
1.3 Sequence timeline: first/last capture times, real-time duration, inter-frame interval, exposure program
1.3.1 Fix interval: use consecutive frame times (even sampling inflated gaps, e.g. 3s→20s)
1.4 Omakase frame-accurate player for in/out + representative frame picking; metadata fields imgseq_inframe / imgseq_outframe
1.5 AI metadata from representative frame via openai_gpt/Ollama (offline job when rep frame is set)
1.6 Scan roots are read-only: manifests in filestore, web staging under $storagedir (never write/delete in $syncdir)
1.6.1 Allow ExifTool/FFmpeg shell paths under $image_sequence_sync_roots (external volumes with spaces in folder names)
1.6.2 Batch ExifTool date reads when listing stills; year-folder sync prints per-shoot progress
1.7 View metadata tabs: Default (descriptive), Sequence (timing/edit points), Image (camera/EXIF)
1.8 On ingest, write folder name, full folder path, and sequence code (name before first space/dash/underscore) to metadata
1.9 Search cards show "Sequence" instead of "JSON" for Image Sequence resources
1.10 Search cards show sequence code as a pill beside status
1.11 Representative frame saved as full-res alternative file in filestore (replaced when changed)
