name: image_sequence
title: Image Sequence
author: Dave Klee
version: 1.17
desc: Ingest still folders as Image Sequence resources (one folder = one sequence; EXIF first/last + cadence samples; optional later shot-split). Read-only scan roots, filestore manifests/staging, Omakase in/out + representative frame, video NLE, search-card hover scrub. Requires the StaticSync staticsync_skip_file hook — see readme.txt / patches/.
icon: images
icon-colour: steelblue
config_url: /plugins/image_sequence/pages/setup.php
category: Asset Processing
default_priority: 998
disable_group_select: 1
