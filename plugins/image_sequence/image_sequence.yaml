name: image_sequence
title: Image Sequence
author: Dave Klee
version: 1.15
desc: Ingest folders of stills as Image Sequence resources (read-only scan roots, filestore manifests/staging, metadata tabs, folder name/path/sequence-code on ingest, cadence auto-split, Omakase frame-accurate in/out + representative frame as full-res alt file, same NLE on video resources with still extraction, sequence timeline/exposure analysis, AI metadata from representative frame via Ollama). Requires the StaticSync staticsync_skip_file hook — see readme.txt / patches/.
icon: images
icon-colour: steelblue
config_url: /plugins/image_sequence/pages/setup.php
category: Asset Processing
default_priority: 998
disable_group_select: 1
